name: Auto Tag on Release Merge

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  auto-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for proper tagging

      - name: Check if this is a release merge
        id: check_release
        run: |
          # Get the commit message
          COMMIT_MSG=$(git log -1 --pretty=%B)
          echo "Commit message: $COMMIT_MSG"

          # Check if commit message starts with "Release v"
          if [[ "$COMMIT_MSG" =~ ^Release\ v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            echo "is_release=true" >> $GITHUB_OUTPUT
            # Extract version from commit message (e.g., "Release v0.6.0 - Description")
            VERSION=$(echo "$COMMIT_MSG" | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | head -1)
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "Detected release version: $VERSION"
          else
            echo "is_release=false" >> $GITHUB_OUTPUT
            echo "Not a release commit, skipping tag creation"
          fi

      - name: Check if tag already exists
        if: steps.check_release.outputs.is_release == 'true'
        id: check_tag
        run: |
          VERSION="${{ steps.check_release.outputs.version }}"
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            echo "tag_exists=true" >> $GITHUB_OUTPUT
            echo "Tag $VERSION already exists, skipping"
          else
            echo "tag_exists=false" >> $GITHUB_OUTPUT
            echo "Tag $VERSION does not exist, will create"
          fi

      - name: Extract CHANGELOG for this version
        if: steps.check_release.outputs.is_release == 'true' && steps.check_tag.outputs.tag_exists == 'false'
        id: changelog
        run: |
          VERSION="${{ steps.check_release.outputs.version }}"
          # Remove 'v' prefix for CHANGELOG lookup (CHANGELOG uses [0.6.0] not [v0.6.0])
          VERSION_NO_V="${VERSION#v}"

          # Extract the section for this version from CHANGELOG.md using line numbers
          # This approach is more reliable than awk patterns with shell variables
          START_LINE=$(grep -n "^## \[${VERSION_NO_V}\]" CHANGELOG.md | cut -d: -f1)

          if [ -z "$START_LINE" ]; then
            echo "Warning: Could not find CHANGELOG entry for version $VERSION_NO_V"
            echo "Release $VERSION" > tag_message.txt
          else
            # Find the next version header after START_LINE
            END_LINE=$(grep -n "^## \[" CHANGELOG.md | awk -F: -v start="$START_LINE" '$1 > start {print $1; exit}')

            if [ -z "$END_LINE" ]; then
              # No next version found, extract to end of file
              sed -n "${START_LINE},\$p" CHANGELOG.md | grep -v '^##' | awk 'NF' > tag_message.txt
            else
              # Extract between START_LINE and END_LINE (exclusive)
              sed -n "${START_LINE},$((END_LINE-1))p" CHANGELOG.md | grep -v '^##' | awk 'NF' > tag_message.txt
            fi
          fi

          if [ ! -s tag_message.txt ]; then
            echo "Warning: CHANGELOG extraction produced empty content"
            echo "Release $VERSION" > tag_message.txt
          fi

          echo "Tag message prepared from CHANGELOG"
          echo "Content preview:"
          head -10 tag_message.txt

      - name: Create and push tag
        if: steps.check_release.outputs.is_release == 'true' && steps.check_tag.outputs.tag_exists == 'false'
        run: |
          VERSION="${{ steps.check_release.outputs.version }}"

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create annotated tag with CHANGELOG message
          # Use --cleanup=verbatim to preserve all content exactly as written
          git tag -a "$VERSION" --cleanup=verbatim -F tag_message.txt

          # Push tag (this will trigger the release workflow)
          git push origin "$VERSION"

          echo "Created and pushed tag $VERSION"
          echo "Release workflow will now build and publish binaries"
