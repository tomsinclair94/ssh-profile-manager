# SSH Profile Manager v0.7.0 - Phase 6: Encryption & Security

**Status:** 🔄 In Progress (6A-6D Complete, 6E: Testing)
**Created:** 2026-01-31
**Last Updated:** 2026-02-04

---

## Overview

Phase 6 implements robust encryption for export/import functionality using industry-standard cryptography. All exports containing profiles with password authentication are mandatorily encrypted to protect sensitive credentials.

**Key Features:**
- AES-256-GCM authenticated encryption
- PBKDF2-HMAC-SHA256 key derivation (600k iterations - OWASP 2023)
- Additional HMAC-SHA256 integrity verification (fail-fast)
- Password strength meter with visual feedback (non-enforcing)
- Mandatory encryption when password-based profiles are included
- Progress indicators for encryption/decryption operations

---

## Architecture Overview

### Encryption Flow
```
User Password (12+ chars)
    ↓
PBKDF2-HMAC-SHA256 (600k iterations, random 16-byte salt)
    ↓
256-bit Encryption Key
    ↓
Export Data → JSON serialize → AES-256-GCM encrypt (random 12-byte IV)
    ↓
Ciphertext + Auth Tag
    ↓
HMAC-SHA256 over (salt + IV + ciphertext)
    ↓
Base64 encode all components
    ↓
EncryptedExport struct
```

### Decryption Flow
```
EncryptedExport
    ↓
Parse and decode Base64 components
    ↓
Verify HMAC-SHA256 (fail fast if tampered)
    ↓
PBKDF2-HMAC-SHA256 (user password + stored salt)
    ↓
AES-256-GCM decrypt (verify auth tag)
    ↓
JSON deserialize
    ↓
Import data structures
```

### Data Structure

```rust
struct EncryptedExport {
    version: String,          // Export format version (e.g., "2.0")
    encrypted: bool,          // Always true for encrypted exports
    cipher: String,           // "AES-256-GCM"
    kdf: String,              // "PBKDF2-HMAC-SHA256"
    kdf_iterations: u32,      // 600000
    salt: String,             // Base64-encoded 16-byte salt
    iv: String,               // Base64-encoded 12-byte IV (nonce)
    data: String,             // Base64-encoded ciphertext
    hmac: String,             // Base64-encoded HMAC-SHA256
}
```

---

## Implementation Sub-Phases

### Phase 6A: Backend Encryption Infrastructure ✅ Complete (2026-02-02)

Core crypto primitives added to `src-tauri/src/lib.rs`: `EncryptedExport` struct, `derive_key()` (PBKDF2-HMAC-SHA256, 600k iterations), `encrypt_data()` (AES-256-GCM + random salt/IV + zeroization), `decrypt_data()` (HMAC-first verification + zeroization), `generate_hmac()`/`verify_hmac()` (constant-time comparison), `validate_encryption_password()` (12+ chars). Dependencies added to `Cargo.toml`: aes-gcm, pbkdf2, sha2, hmac, zeroize.

### Phase 6B: Export Command Integration ✅ Complete (2026-02-02)

Added `encryption_password: Option<String>` to `export_profile`, `export_group`, `export_profiles`. Added `export_requires_encryption()` helper (returns true if any profile has `auth_method == "password"`). Mandatory encryption enforced when password profiles included; encryption optional for key-auth-only exports.

### Phase 6C: Import Command Integration ✅ Complete (2026-02-03)

Added `detect_encrypted_export()` (checks `encrypted: true` field) and `decrypt_import_if_encrypted()` (single entry point for all import commands). Updated `import_profile`, `import_group`, `import_profiles` with `encryption_password: Option<String>`. Backward compatible: plain v0.6.x and unencrypted v0.7.0 exports pass through unchanged.

### Phase 6D: Frontend UI for Encryption ✅ Complete (2026-02-04)

Two new modals in `dist/index.html`: encryption password modal (two inputs + strength meter) and decryption password modal (single input). `dist/main.js` updated with `exportNeedsEncryption()`, `calculatePasswordStrength()`, `openEncryptionPasswordModal()`/`openDecryptionPasswordModal()` (Promise-based), and `routeEncryptedImport()` (probes profile → group → all profiles; crypto errors surfaced immediately, parse errors fall through). All 3 export functions prompt when password-auth profiles included. Strength meter uses CSS classes only (CSP-compliant). Modal stack integration for both modals.

---

### Phase 6E: Security Validation & Testing 🔄 Current

**Goal:** Comprehensive testing and security review of all Phase 6 functionality.

**Files to review:** `src-tauri/src/lib.rs` (crypto functions + export/import commands), `dist/main.js` (encryption UI + import routing), `dist/index.html` (encryption/decryption modals), `dist/styles.css` (strength meter styles)

---

#### Backend Tests

*Pure-function tests (password validation, round-trips, HMAC tampering, wrong password, helper functions) are automated — run via `cargo test`. Items that depend on database state (group/backup round-trips, mandatory encryption enforcement, backward compatibility) remain manual via the running app.*

**Encrypt/Decrypt Round-Trips:**
- [x] Single profile: encrypt with 12-char password → export file → import with same password → verify data matches
- [ ] Group with sub-groups: encrypt → export → import → verify all profiles and sub-groups present
- [ ] Full backup: encrypt all profiles → export → import → verify complete restore
- [x] Special characters in password (Unicode, symbols) → encrypt/decrypt succeeds

**HMAC Tampering Detection:**
- [x] Modify `hmac` field in exported JSON → import fails with integrity error
- [x] Modify `data` field in exported JSON → HMAC verification fails
- [x] Modify `salt` field in exported JSON → HMAC verification fails
- [x] Modify `iv` field in exported JSON → HMAC verification fails
- [x] Verify error messages are clear and do not leak internal details

**Wrong Password:**
- [x] Encrypt with one password → decrypt with a different password → "Incorrect password" error
- [x] Multiple wrong attempts produce the same error (no information leakage)

**Password Validation:**
- [x] Empty password → validation error
- [x] 11-character password → validation error
- [x] 12-character password → accepted
- [x] Unicode password (12+ chars) → accepted

**Mandatory Encryption Enforcement:**
- [ ] Export password-auth profile with "Include passwords" checked, no encryption password → error returned
- [ ] Export group containing 1 password-auth profile among key-auth profiles → encryption required
- [ ] Export all profiles where all use key-auth → encryption not required (optional)

**Backward Compatibility:**
- [ ] Import a plain v0.6.x export (no `encrypted` field) → imports successfully, no decryption attempted
- [ ] Import a plain v0.7.0 export (unencrypted, key-auth only) → imports successfully

---

#### UI Tests

*Manual testing in the running application.*

**Export Flow:**
- [ ] Export key-auth profile with "Include passwords" checked → no encryption prompt, plain JSON saved
- [ ] Export password-auth profile with "Include passwords" checked → encryption modal appears
- [ ] Export password-auth profile without "Include passwords" checked → no encryption prompt, plain JSON saved
- [ ] Export group containing password-auth profiles with "Include passwords" checked → encryption modal appears
- [ ] Export all profiles (some password-auth) with "Include passwords" checked → encryption modal appears

**Encryption Modal:**
- [ ] Password < 12 characters → "Password must be at least 12 characters" error shown
- [ ] Passwords do not match → "Passwords do not match" error shown
- [ ] Strength meter shows Weak (red) for short or simple password
- [ ] Strength meter shows Fair (orange) for moderate password
- [ ] Strength meter shows Good (yellow) for good password
- [ ] Strength meter shows Strong (green) for 16+ char password with mixed character classes
- [ ] Escape key closes modal without exporting
- [ ] Tab cycles through: password input → confirm input → Submit → Cancel

**Import Flow:**
- [ ] Import encrypted file → decryption modal appears automatically
- [ ] Import plain (unencrypted) file → no decryption modal, imports normally
- [ ] Import encrypted single profile (no name conflict) → imports successfully after correct password
- [ ] Import encrypted single profile (name conflict) → conflict dialog appears with generic name
- [ ] Import encrypted group → imports successfully after correct password
- [ ] Import encrypted all-profiles → confirmation dialog appears, then imports after correct password

**Decryption Modal:**
- [ ] Wrong password entered → "Incorrect password" error shown, modal stays open
- [ ] Correct password entered → import proceeds
- [ ] Escape key closes modal without importing
- [ ] Tab cycles through: password input → Submit → Cancel

**Spinners & Feedback:**
- [ ] Encryption spinner visible during export operation
- [ ] Decryption spinner visible during import operation
- [ ] All error messages are clear, actionable, and in British English

---

#### Security Review

- [ ] Run `security-engineer` agent on all Phase 6 code (`src-tauri/src/lib.rs` crypto functions, `dist/main.js` encryption UI)
- [ ] Review and fix any CRITICAL or HIGH findings immediately
- [ ] Document MEDIUM/LOW findings for future consideration

**Memory & Code Audit (manual review):**
- [ ] Verify `.zeroize()` called on all derived keys after use
- [ ] Verify `.zeroize()` called on password buffers after use
- [ ] Verify no passwords or keys appear in log statements or error messages
- [ ] Verify constant-time comparison used for HMAC verification (not byte-by-byte `==`)
- [ ] Verify British English in all encryption/decryption UI strings

---

#### Phase 6E Success Criteria

Phase 6E is complete when all checkboxes above are ticked and:
- [ ] All backend round-trip tests pass
- [ ] All HMAC tampering scenarios detected correctly
- [ ] All UI flows work as expected (export prompt, import prompt, conflict resolution)
- [ ] Security-engineer agent review complete with no unresolved CRITICAL/HIGH issues
- [ ] Memory safety confirmed (no password/key leaks)
- [ ] British English verified across all encryption UI

---

## Security Considerations

### Cryptographic Choices

**Why AES-256-GCM?**
- Industry standard for authenticated encryption
- Single primitive for confidentiality + integrity
- Hardware acceleration on modern CPUs
- Recommended by NIST, OWASP

**Why PBKDF2 with 600k iterations?**
- OWASP 2023 recommendation
- Sufficient resistance to brute-force attacks
- ~1-2 seconds on modern hardware (acceptable UX with spinner)

**Why additional HMAC-SHA256?**
- Defense-in-depth: AES-GCM auth tag + separate HMAC
- Fail-fast verification before decryption attempt
- Prevents certain cryptographic attacks on GCM

### Threat Model

**Protected Against:**
- Brute-force password attacks (600k iterations + strong password recommendation)
- Data tampering (HMAC verification + GCM auth tag)
- Chosen-ciphertext attacks (authenticated encryption)
- Timing attacks (constant-time HMAC comparison)
- Password leakage (zeroization after use)

**NOT Protected Against:**
- Weak user passwords (users can ignore strength meter)
- Keyloggers/screen capture (out of scope)
- Compromised devices (OS-level threat)

### Best Practices Implemented

1. **Random Salt/IV** - Generated per export (prevents rainbow tables, replay attacks)
2. **Zeroization** - Passwords/keys cleared from memory immediately after use
3. **Fail-Fast** - HMAC verified before attempting decryption
4. **Constant-Time Comparison** - HMAC verification prevents timing attacks
5. **Version Tracking** - Export format includes crypto parameters for future-proofing
6. **Strong Defaults** - 600k iterations, 256-bit keys, 12-byte IVs (OWASP/NIST compliant)

---

## Error Handling Reference

### Export Errors

| Error | User-Facing Message |
|-------|---------------------|
| Missing encryption password | "Encryption required: This export contains password-authenticated profiles. Please provide an encryption password." |
| Password too short | "Password must be at least 12 characters." |
| Passwords don't match | "Passwords do not match. Please re-enter." |
| Encryption failure | "Encryption failed: [detail]. Please try again." |

### Import Errors

| Error | User-Facing Message |
|-------|---------------------|
| No password provided | "This import is encrypted. Please provide the decryption password." |
| Wrong password | "Incorrect password. Please try again." |
| HMAC mismatch | "Import file is corrupted or has been tampered with. Cannot import." |
| Corrupted data | "Import file is corrupted or invalid. Cannot import." |

---

## Known Limitations (Acceptable for v0.7.0)

- **No password recovery:** If user forgets encryption password, data is unrecoverable (by design)
- **No password complexity enforcement:** Users can choose weak passwords (strength meter guides but doesn't block)
- **Single encryption method:** AES-256-GCM only, no cipher selection
- **No key rotation:** Re-encryption requires full decrypt → re-encrypt cycle

**Future Considerations (v0.8.0+):**
- Encrypted local database (not just exports)
- Cloud backup with end-to-end encryption
- Hardware security module integration

---

## Rollback Plan

If Phase 6 has critical issues:

1. Remove encryption parameters from export/import commands (restore plain JSON behaviour)
2. Remove crypto dependencies from Cargo.toml
3. Hide encryption UI elements (CSS display: none)
4. Plain export/import continues to work — encryption is purely additive

---

## Post-Phase 6 Tasks

Before merging to main:

1. Update CHANGELOG.md:
   - "Added: Encrypted exports with AES-256-GCM encryption"
   - "Added: Password strength meter for encryption passwords"
   - "Added: Mandatory encryption for password-authenticated profile exports"
   - "Added: HMAC integrity verification for encrypted imports"
2. Verify CLAUDE.md command signatures are up to date (already updated in Phase 6B/6C)
3. Proceed to Phase 8 (Testing & Documentation)

---

**Last Updated:** 2026-02-04
**Next Phase:** Phase 8 (Testing & Documentation)
