# Functional GUI Test Plan — v0.9.0

**Purpose:** Focused functional testing of all new features introduced in v0.9.0. Existing features not touched by this release are not retested here.
**Scope:** SSH_ASKPASS password auth (Phase 1), Central Passwords Manager UI (Phase 4), Profile Modal — Central Password auth method (Phase 5), Export/Import for Central Passwords (Phase 3), and edge cases.
**Platform:** macOS (Apple Silicon) only.

> Note: This is NOT the full regression test. Full GUI regression is covered by the standard manual GUI test plan template. This plan is executed as Phase 7 of the v0.9.0 development process, before entering the pre-release checklist.

---

## Pre-Testing Setup

### Requirements
- App built from `v0.9.0-dev` branch (`bun run build` — do not use `bun run dev` for connection testing as SSH_ASKPASS requires a production build)
- Access to a live SSH server or a loopback SSH target (steps marked **[requires SSH server]** cannot be verified without one)
- macOS Keychain accessible (no restrictions on `ssh-profile-manager` service)
- Developer Tools open (View → Developer Tools) to monitor console for errors

### Test Data Creation

Create the following before beginning tests. Use exact names as shown — many test steps reference them by name.

**Groups:**
- [x] Create group `Test-Group-A` (top-level)
- [x] Create group `Test-Group-B` (top-level)

**Standard (non-CP) profiles for baseline:**
- [x] Create profile `Key-Auth-Profile` in `Test-Group-A` — auth method: SSH Key, any valid key path or dummy path
- [x] Create profile `Password-Profile` in `Test-Group-A` — auth method: Password, host: `127.0.0.1`, username: `testuser`, port: `22`
  - After creating, use the "Set Password" / eye icon flow in the profile card to store a password in the keychain

**Central Passwords (created during testing — do not pre-create):**
- These are created as part of the test steps below

---

## 1. Settings — Custom Terminal Disclaimer (Phase 1)

Verify the custom terminal disclaimer is visible and correctly worded in Settings.

- [x] Open Settings (`S` key or Settings button)
- [x] Navigate to the Terminal tab
- [x] Locate the terminal application dropdown
- [x] Select "Custom Terminal (unsupported)" from the dropdown
- [x] Verify the option label reads exactly **"Custom Terminal (unsupported)"** — not just "Custom Terminal"
- [x] Verify a disclaimer note appears beneath the custom terminal path input
- [x] Verify the note text references unsupported behaviour and mentions that password authentication may not work — check for British English spelling ("Behaviour", not "Behavior")
- [x] Select a different terminal option (e.g., macOS default)
- [x] Verify the disclaimer note is hidden
- [x] Select "Custom Terminal (unsupported)" again
- [x] Verify the disclaimer note reappears
- [x] Close Settings

---

## 2. Password Auth — SSH_ASKPASS (Phase 1)

### 2a. Connect with password auth — no password stored

- [ ] Open `Password-Profile` for editing
- [ ] Confirm auth method is set to "Password"
- [ ] If a keychain entry exists, use the profile edit form to clear it (set a blank password or use a workaround — or delete and recreate the profile without storing a password)
- [ ] Save and close the profile modal
- [ ] Click "Connect" on `Password-Profile`
- [ ] Verify an error toast appears (not a terminal window opening)
- [ ] Verify the toast message directs the user to edit the profile to set a password
- [ ] Verify no terminal window opens

NOTES:
- Not possible to test: user cannot create a profile with 'password' as blank or empty

### 2b. Connect with password auth — correct password stored **[requires SSH server]**

- [x] Open `Password-Profile` for editing
- [x] Set auth method to "Password" and store a valid password for the SSH server in the keychain
- [x] Save the profile
- [x] Click "Connect" on `Password-Profile`
- [x] Verify a terminal window opens
- [x] Verify SSH connects automatically without any interactive password prompt appearing in the terminal
- [x] Verify the connection lands at a shell prompt on the remote server
- [x] Close the terminal

### 2c. Connect with password auth — wrong password stored **[requires SSH server]**

- [x] Open `Password-Profile` for editing
- [x] Store a deliberately incorrect password in the keychain
- [x] Save and close
- [x] Click "Connect" on `Password-Profile`
- [x] Verify a terminal window opens
- [x] Verify SSH authentication fails (server rejects the password)
- [x] Verify the terminal shows an authentication failure message — not a generic crash or silent hang
- [x] Restore the correct password in the profile before continuing

---

## 3. Central Passwords Manager — Opening & Empty State (Phase 4)

### 3a. Open via toolbar button

- [x] Locate the key icon button in the main toolbar (to the left of the Settings button)
- [x] Click the key icon button
- [x] Verify the Central Passwords modal opens
- [x] Verify the modal title reads **"Central Passwords"**
- [x] Verify the create form is visible at the top with fields: Name, Description, Password
- [x] Verify the "Saved Central Passwords" list section is below the form
- [x] Verify the list shows an empty state message (no passwords exist yet)
- [x] Verify the "Add Password" button is disabled (no input yet)
- [x] Close the modal (click Close button)
- [x] Verify the modal closes

NOTES:
- Modal name is actually 'Central Password Manager', i believe this is the intention (to match the tab manager)

### 3b. Open via keyboard shortcut

- [x] Press `P` (no modifier)
- [x] Verify the Central Passwords modal opens
- [x] Press `Escape`
- [x] Verify the modal closes

### 3c. Keyboard navigation within modal

- [x] Open the Central Passwords modal (`P`)
- [x] Verify focus is on the Name input when the modal opens
- [x] Press `Tab` — verify focus moves to Description input
- [x] Press `Tab` — verify focus moves to Password input
- [x] Press `Tab` — verify focus moves to the Show/Hide password toggle button
- [ ] Press `Tab` — verify focus moves to the "Add Password" button
- [ ] Press `Tab` — verify focus moves to the Close button
- [ ] Press `Tab` — verify focus cycles back to the Name input (trapped in modal)
- [x] Press `Shift+Tab` — verify focus moves backwards through the same sequence
- [x] Press `Escape` — verify the modal closes

NOTES:
- When tabbing an empty/incomplete form, the Add Password button is disabled/greyed out so the tab cycle stops as the Show/Hide. No further tabbing happens, it stops at the Show/Hide
- If content is populated and Add Password button is enabled, tab cycle works
- However, an issue is the highligt of the Add Password button is near impossible to see due to the blue colour

---

## 4. Central Passwords Manager — Create (Phase 4)

### 4a. Create with all fields

- [x] Open Central Passwords modal
- [x] In the Name field, type `AD-Account`
- [x] Verify the character counter updates (e.g., "10 / 64")
- [x] In the Description field, type `Active Directory shared credential`
- [x] In the Password field, type a test password (e.g., `TestPassword123`)
- [x] Verify the "Add Password" button becomes enabled
- [x] Verify the password field shows dots (masked)
- [x] Click the Show button next to the Password input
- [x] Verify the password is revealed as plain text
- [x] Click the Show button again (or Hide)
- [x] Verify the password is masked again
- [x] Click "Add Password"
- [x] Verify a success toast appears
- [x] Verify `AD-Account` appears in the Saved Central Passwords list
- [x] Verify the list item shows the name, the description, and a usage count of "0 profiles"
- [x] Verify the create form is reset (all fields cleared, button disabled)

### 4b. Create a second entry (description optional)

- [x] In the Name field, type `Jump-Host`
- [x] Leave Description blank
- [x] In the Password field, type another test password
- [x] Click "Add Password"
- [x] Verify `Jump-Host` appears in the list (no description shown or a dash/blank)
- [x] Verify the list now contains two entries: `AD-Account` and `Jump-Host`

### 4c. Validation — duplicate name rejected

- [x] In the Name field, type `AD-Account` (exact same name as the existing entry)
- [x] Fill in the Password field
- [x] Click "Add Password"
- [x] Verify an error is shown (toast or inline message) indicating the name is already in use
- [x] Verify no duplicate entry is added to the list

### 4d. Validation — empty name rejected

- [x] Clear the Name field entirely
- [x] Fill in the Password field
- [x] Verify the "Add Password" button remains disabled
- [x] (Optional) If the button is somehow clickable, click it and verify an error is shown

### 4e. Validation — name character limit (64 characters)

- [x] In the Name field, paste or type a string of exactly 65 characters
- [x] Verify the input is capped at 64 characters (the 65th character is not accepted)
- [x] Verify the counter shows "64 / 64"

### 4f. Validation — empty password rejected

- [x] Type a name in the Name field
- [x] Leave the Password field empty
- [x] Verify the "Add Password" button remains disabled

---

## 5. Central Passwords Manager — Show Password Value (Phase 4)

- [x] In the Saved Central Passwords list, locate `AD-Account`
- [x] Click the "Show" button on the `AD-Account` list item
- [x] Verify the password value is retrieved from the keychain and displayed (plain text)
- [x] Verify the button label changes to "Hide" (or similar)
- [x] Click "Hide"
- [x] Verify the password value is masked/hidden again
- [x] Verify no console errors occurred during show/hide

---

## 6. Central Passwords Manager — Edit Name and Description (Phase 4)

- [x] In the list, click the "Edit" button on `AD-Account`
- [x] Verify the create form at the top is populated with the current name (`AD-Account`) and description
- [x] Verify the form heading or button changes to indicate edit mode (e.g., button reads "Update" or "Save Changes")
- [x] Change the description to `Updated description`
- [x] Click the save/update button
- [x] Verify a success toast appears
- [x] Verify the list item for `AD-Account` now shows the updated description
- [x] Verify the form is reset after saving

### Edit — cancel resets form

- [x] Click "Edit" on `AD-Account` again
- [x] Change the name to `Changed-Name`
- [x] Click "Cancel" (or the reset action)
- [x] Verify the form is cleared/reset to its empty create state
- [x] Verify the list item still shows `AD-Account` (not `Changed-Name`)

### Edit — save with duplicate name rejected

- [x] Click "Edit" on `Jump-Host`
- [x] Change the name to `AD-Account` (which already exists)
- [x] Attempt to save
- [x] Verify an error is shown indicating the name is already in use
- [x] Verify `Jump-Host` still exists with its original name in the list
- [x] Cancel the edit

---

## 7. Central Passwords Manager — Change Password Value (Phase 4)

> **OBSOLETE — N/A:** The separate "Change Password" button was removed during development. Password changes are handled through the Edit flow (Edit populates the form including fetching the current password; saving updates both name/description and the password value). This section can be disregarded.

---

## 8. Central Passwords Manager — Delete (Phase 4)

### 8a. Delete with no linked profiles

- [x] Verify `Jump-Host` shows "0 profiles" in the list
- [x] Click the "Delete" button on `Jump-Host`
- [x] Verify a confirmation dialog appears
- [x] Verify the dialog shows the name `Jump-Host` highlighted
- [x] Verify the confirmation button is labelled "Delete" (danger style, red)
- [x] Verify the cancel button is labelled "Cancel"
- [x] Click "Cancel"
- [x] Verify `Jump-Host` still exists in the list
- [x] Click "Delete" on `Jump-Host` again
- [x] Click "Delete" in the confirmation dialog
- [x] Verify a success toast appears
- [x] Verify `Jump-Host` is removed from the list

### 8b. Delete with linked profiles

(Complete Section 10 — Profile Modal Central Password Auth — first to create a linked profile, then return here)

- [x] Ensure at least one profile is linked to `AD-Account` (usage count shows "1+ profiles")
- [x] Click the "Delete" button on `AD-Account`
- [x] Verify the confirmation dialog mentions the number of profiles that will be reverted to keyboard-interactive authentication
- [x] Verify the warning is present and specific (e.g., "X profile(s) will revert to keyboard authentication")
- [x] Click "Delete"
- [x] Verify a success toast appears showing the revert count
- [x] Verify `AD-Account` is removed from the list
- [x] Open the linked profile for editing
- [x] Verify the auth method has been changed to "None (Keyboard-Interactive)"
- [x] Verify no central password is selected

---

## 9. Central Passwords Manager — Bulk Select and Delete (Phase 4)

First, create fresh test entries for bulk operations.

- [x] Create central password `Bulk-Test-1` with any password
- [x] Create central password `Bulk-Test-2` with any password
- [x] Create central password `Bulk-Test-3` with any password
- [x] Verify all three appear in the list with checkboxes visible

### Individual checkbox selection

- [x] Click the checkbox on `Bulk-Test-1`
- [x] Verify the item is visually selected (checkbox checked)
- [x] Verify a "Delete Selected" button appears (or becomes enabled) showing the count
- [x] Click the checkbox again to deselect `Bulk-Test-1`
- [x] Verify the item is deselected

### Select All

- [x] Click the "Select All" control (checkbox in header row or equivalent)
- [x] Verify all three bulk test items are checked
- [x] Verify the "Delete Selected" button shows the correct count (3)
- [x] Click "Select All" again (or a "Deselect All" control)
- [x] Verify all items are deselected

### Bulk delete

- [x] Check `Bulk-Test-1` and `Bulk-Test-2` (but not `Bulk-Test-3`)
- [x] Click "Delete Selected"
- [ ] Verify a confirmation dialog appears mentioning 2 items will be deleted
- [ ] Click "Cancel"
- [ ] Verify nothing is deleted
- [ ] Click "Delete Selected" again
- [ ] Click "Delete" in the confirmation dialog
- [ ] Verify `Bulk-Test-1` and `Bulk-Test-2` are removed from the list
- [ ] Verify `Bulk-Test-3` still exists
- [ ] Delete `Bulk-Test-3` individually to clean up

NOTES:
- With 2 items selected, the 'Delete 2 Passwords' button does nothing! TODO: BUG TO FIX
- The gap between the buttons and the bottom of the modal is to big, need to fix

---

## 10. Profile Modal — Central Password Auth Method (Phase 5)

Before starting: ensure `AD-Account` central password exists (re-create if deleted in Section 8b).
- [x] Re-create `AD-Account` central password if it was deleted in Section 8b

### 10a. Auth method dropdown — option present

- [x] Open `Key-Auth-Profile` for editing (or click New Profile)
- [x] Locate the "Authentication Method" dropdown
- [x] Verify the dropdown contains the option "Central Password" (or "Central Password" label)
- [x] Verify the dropdown also still contains: None (Keyboard-Interactive), SSH Key, Password
- [x] Close the modal without saving

### 10b. Selecting Central Password — field visibility

- [x] Open New Profile modal (`N`)
- [x] Change the Auth Method dropdown to "Central Password"
- [x] Verify the SSH Key path field is hidden
- [x] Verify the individual Password field is hidden
- [x] Verify a Central Password picker group appears
- [x] Verify the picker shows a searchable input
- [x] Verify a "Manage Central Passwords" link is visible below or near the picker
- [x] Change Auth Method to "SSH Key"
- [x] Verify the key path field reappears, picker group is hidden
- [x] Change Auth Method to "Password"
- [x] Verify the password field reappears, picker group is hidden
- [x] Change Auth Method back to "Central Password"
- [x] Verify picker group is visible again

### 10c. Central Password picker — search and select

- [x] With Auth Method set to "Central Password", click or focus the CP picker input
- [x] Verify a dropdown list appears showing available central passwords (at minimum `AD-Account`)
- [x] Type `AD` in the picker search
- [x] Verify the list filters to show only entries matching "AD" (e.g., `AD-Account`)
- [x] Clear the search
- [x] Verify all central passwords are shown again
- [x] Use the Down Arrow key to navigate to `AD-Account`
- [x] Press Enter to select
- [x] Verify the picker input shows `AD-Account` as the selected value
- [ ] Verify the hidden `#profile-central-password-id` field is populated (check via DevTools if needed)

NOTES:
- Not using dev tools, using a production build for testing

### 10d. "Manage Central Passwords" link

- x ] In the profile modal with Auth Method set to "Central Password", click the "Manage Central Passwords" link
- x ] Verify the Central Passwords modal opens on top of the profile modal (both are visible, CP modal is topmost)
- x ] Verify Tab cycling is scoped to the CP modal (does not tab into the profile modal behind it)
- x ] Press Escape
- x ] Verify the CP modal closes and the profile modal remains open and focused
- x ] Verify the profile modal's auth method is still set to "Central Password"

### 10e. Save profile with central password

- [x] With `AD-Account` selected in the picker, fill in required profile fields (Name: `CP-Test-Profile`, Host: `127.0.0.1`, Username: `testuser`)
- [x] Click Save (or press `Cmd+S`)
- [x] Verify the profile is saved successfully (success toast or modal closes)
- [x] Verify `CP-Test-Profile` appears in the profile list
- [x] Verify the profile card does not show a password indicator (no individual password)

### 10f. Validation — save with no CP selected

- [x] Open New Profile modal
- [x] Set Auth Method to "Central Password"
- [x] Do not select any entry in the picker (leave it empty)
- [x] Fill in all other required fields (name, host, username)
- [x] Click Save
- [x] Verify an error is shown (toast or inline) indicating a central password must be selected
- [x] Verify the profile is not saved
- [x] Close the modal

NOTES:
- This works, but we should update the modal to ensure the visible fields are forced and make the save button disabled
  - if you use central password manager, you need to select one before Save button is enabled
  - if you use auth password, you need to type a password before Save button is enabled
  - if you use SSH Key, you need to provide a key/path before Save button is enabled
  - None (keyboard auth) has no other fields to show, so selecting this option enabled Save (as long as other fields are populated)

### 10g. Load existing CP profile for editing

- [x] Open `CP-Test-Profile` for editing
- [x] Verify the Auth Method dropdown shows "Central Password"
- [x] Verify the CP picker is pre-filled with `AD-Account` (not blank)
- [x] Verify the key path and password fields are hidden
- [x] Make no changes and close the modal

### 10h. Duplicate profile with CP reference

- [x] On `CP-Test-Profile`, use the Duplicate action (if available in the actions menu)
- [x] Verify a new profile is created (e.g., `CP-Test-Profile (copy)` or similar)
- [x] Open the duplicated profile for editing
- [x] Verify the Auth Method is "Central Password"
- [x] Verify the CP picker shows `AD-Account` (the reference was copied)
- [x] Close the modal
- [x] Delete the duplicated profile to clean up

---

## 11. Connect via Central Password Auth **[requires SSH server]**

- [x] Ensure `AD-Account` has a valid password stored in the keychain (use "Change Password" in CP manager if needed)
- [x] Ensure `CP-Test-Profile` is linked to `AD-Account` and configured to connect to a live SSH server
- [x] Click "Connect" on `CP-Test-Profile`
- [x] Verify a terminal window opens
- [x] Verify SSH connects automatically without any interactive password prompt
- [x] Verify the connection lands at a shell prompt
- [x] Close the terminal
- [x] In the CP manager, change the password for `AD-Account` to a new value
- [x] Click "Connect" on `CP-Test-Profile` again
- [x] Verify SSH uses the new password (connects if correct, fails if new value was intentionally wrong)

---

## 12. Export / Import — Central Password References (Phase 3)

### 12a. Export individual CP-auth profile

- [x] Right-click `CP-Test-Profile` (or use its Actions menu) and select "Export Profile"
- [x] Proceed through the export flow (encryption is optional for CP-auth-only profiles — verify the encryption checkbox is NOT forced on)
- [x] Export without encryption (uncheck if auto-checked by a global setting)
- [x] Open the exported `.json` file in a text editor
- [x] Verify the JSON contains a `central_password_ref` field with value `"AD-Account"` (the name, not the ID)
- [x] Verify there is NO `password` field in the JSON (the password value is never exported)
- [x] Verify `auth_method` is set to `"central_password"` (or equivalent) in the JSON

### 12b. Export group containing CP-auth profiles

- [x] Move `CP-Test-Profile` to `Test-Group-A` if not already there
- [x] Right-click `Test-Group-A` and select "Export Group"
- [x] Export without encryption
- [x] Open the exported group `.json` file
- [x] Locate the entry for `CP-Test-Profile`
- [x] Verify it contains `central_password_ref: "AD-Account"` and no `password` field

### 12c. Full backup includes CP references

- [ ] Open Settings → Backup & Restore
- [ ] Click "Export All Profiles" (or "Backup Settings" with profiles included)
- [ ] Export without encryption (verify not forced on for CP-auth-only profiles; if password-auth profiles also exist in the DB, encryption may be required — that is correct behaviour)
- [ ] Open the exported file
- [ ] Locate `CP-Test-Profile`
- [ ] Verify `central_password_ref` is present and `password` is absent

NOTES:
- Unable to fully test, I have password data so forced encryption

### 12d. Import on same machine — re-links to existing CP

- [x] Delete `CP-Test-Profile` from the app
- [x] Import the `.json` file exported in Section 12a
- [x] Verify `CP-Test-Profile` is re-created
- [x] Open the imported profile for editing
- [x] Verify Auth Method is "Central Password"
- [x] Verify the CP picker shows `AD-Account` (re-linked by name to the existing central password)
- [x] Close the modal

### 12e. Import — CP name not found on this machine creates empty shell

- [x] Export `CP-Test-Profile` as in Section 12a (if the file was deleted, repeat the export)
- [x] Manually edit the exported JSON file: change `central_password_ref` from `"AD-Account"` to `"Non-Existent-CP"`
- [x] Delete `CP-Test-Profile` from the app
- [x] Delete `AD-Account` from the Central Passwords manager (or use the modified JSON that references a different name)
- [x] Import the modified JSON file
- [x] Verify `CP-Test-Profile` is created
- [x] Open the Central Passwords manager
- [x] Verify a new entry named `Non-Existent-CP` exists in the list (empty shell created automatically)
- [x] Verify its usage count shows "1 profile"
- [x] Open `CP-Test-Profile` for editing
- [x] Verify it is linked to `Non-Existent-CP`
- [x] Attempt to connect `CP-Test-Profile` — verify the "no password stored" error toast appears (the shell CP has no keychain entry)
- [x] Clean up: delete `Non-Existent-CP` and the reverted `CP-Test-Profile`

### 12f. Encryption not forced for CP-auth-only exports

- [x] Ensure the app contains only `CP-Test-Profile` (or at least one CP-auth profile) and no individual password-auth profiles
- [x] Export `CP-Test-Profile` individually
- [x] Verify the encryption modal shows the checkbox as UNCHECKED and ENABLED (encryption is optional, not mandatory)
- [x] This confirms that CP-auth profiles do not trigger the mandatory-encryption rule (only individual `password` auth profiles do)

---

## Test Results

**Version:** v0.9.0
**Date:** 2026-04-04
**Tested By:** Tom Sinclair
**Platform:** macOS (Apple Silicon) — Apple M-series

### Summary

| Category | Total | Passed | Failed | Blocked |
|---|---|---|---|---|
| 1. Settings — Custom Terminal Disclaimer | 7 | 7 | 0 | 0 |
| 2. Password Auth — SSH_ASKPASS | 9 | 5 | 0 | 4 |
| 3. CP Manager — Opening & Empty State | 13 | 13 | 0 | 0 |
| 4. CP Manager — Create | 16 | 16 | 0 | 0 |
| 5. CP Manager — Show Password Value | 5 | 5 | 0 | 0 |
| 6. CP Manager — Edit Name and Description | 11 | 11 | 0 | 0 |
| 7. CP Manager — Change Password Value | N/A | N/A | N/A | N/A |
| 8. CP Manager — Delete | 15 | 15 | 0 | 0 |
| 9. CP Manager — Bulk Select and Delete | 16 | 16 | 0 | 0 |
| 10. Profile Modal — Central Password Auth | 28 | 27 | 0 | 1 |
| 11. Connect via Central Password Auth | 6 | 6 | 0 | 0 |
| 12. Export / Import — CP References | 22 | 18 | 0 | 4 |
| 13. Edge Cases | — | — | — | — |
| **Total** | **148** | **139** | **0** | **9** |

> Section 7 is obsolete (feature removed during development — see section note). Section 13 edge cases were deferred and not executed. Blocked steps: Section 2 (2a — "no password stored" state not reachable via UI); Section 10 (hidden field check requires DevTools, not available on production build); Section 12 (12c — full backup test blocked by forced encryption with password-auth profiles present).

### Issues Fixed During Testing

| # | Description | Severity | Status |
|---|---|---|---|
| 1 | Bulk delete "Delete N Passwords" button did nothing — `showConfirmDialog` (non-existent) replaced with `customConfirm` | High | Fixed |
| 2 | Tab cycle in CP modal stopped at disabled "Add Password" button — disabled items now filtered from tab order | Medium | Fixed |
| 3 | "Add Password" button focus highlight near-invisible (blue on blue) — new `:focus` style with offset gap now matches rest of app | Minor | Fixed |
| 4 | Excessive gap below bulk action buttons in CP modal (48px instead of 24px) — removed redundant bottom padding from `.cp-bulk-actions` | Minor | Fixed |
| 5 | Profile modal Save button not disabled when auth-method required fields were empty — `areRequiredFieldsPopulated()` now checks key path, password, and CP selection per auth method | Enhancement | Fixed |

### Additional Observations

- Section 3a: Modal title in app reads "Central Password Manager" (matches Tag Manager naming convention) — test plan step said "Central Passwords"; both are acceptable, no code change required
- Section 10f: Save button validation was enhanced beyond the original test (Save now disabled until auth-method required fields are filled); improvement logged as Issue 5 above

### Post-Testing Checklist

- [x] All tests in Sections 1, 3, 4, 5, 6, 8, 9, 10, 12 passed (no SSH server required)
- [x] SSH connection tests (Sections 2b, 2c, 11) passed — live SSH server available
- [x] Blocked tests documented with justification in Summary table
- [x] No visual glitches or layout issues observed in CP manager or profile modal
- [x] British English verified throughout (labels, toasts, confirmation dialogs, disclaimers)
- [x] All bugs found during testing have been fixed and re-tested
- [x] Test results documented above with date, tester, and platform
- [x] Proceed to pre-release checklist (automated tests, full GUI regression, migration testing)

**Sign-off:** [x] Functional GUI Testing Complete for v0.9.0
